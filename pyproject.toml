[project]
name = "resume-agent"
version = "0.1.0"
description = "A resume generation agent using LLM"
dependencies = [
    "fastapi",
    "uvicorn",
    "httpx",
    "python-dotenv",
    "openai",
    "sse-starlette",
    "langchain",
    "langchain-openai",
    "jinja2>=3.1.6",
]

[tool.uv]
dev-dependencies = []
