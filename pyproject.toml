[project]
name = "resume-agent"
version = "0.1.0"
description = "A resume generation agent using LLM"
dependencies = [
    "fastapi",
    "uvicorn",
    "httpx",
    "requests",  # 用于博查 API 网络搜索
    "python-dotenv",
    "openai",
    "sse-starlette",
    "langchain",
    "langchain-openai",
    "jinja2>=3.1.6",
    "asyncpg>=0.29.0",  # PostgreSQL 異步驅動（用於雙軌架構）
]

[tool.uv]
dev-dependencies = []
